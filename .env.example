# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=development

# Audio Configuration
# For REAL-TIME processing: Use 2000-3000ms (faster suggestions, higher cost)
# For BALANCED: Use 5000ms (default)
# For COST-SAVING: Use 10000ms (slower suggestions, lower cost)
AUDIO_CHUNK_DURATION=2000
AUDIO_SAMPLE_RATE=16000
# AUDIO_DEVICE=default  # Use 'default' for microphone only
# For capturing ALL meeting audio (including other participants):
# - Linux: Use PulseAudio monitor (e.g., 'alsa_output.pci-0000_00_1f.3.analog-stereo.monitor')
# - Windows: Use virtual audio cable
# - Mac: Use BlackHole or Loopback

# AI Configuration
WHISPER_MODEL=whisper-1
# For FASTEST suggestions: Use gpt-3.5-turbo (fastest, good quality)
# For BALANCED: Use gpt-4o-mini (fast, better quality)
# For BEST QUALITY: Use gpt-4o (slower, highest quality)
GPT_MODEL=gpt-3.5-turbo
# Reduce context for faster processing
MAX_CONTEXT_LENGTH=5
